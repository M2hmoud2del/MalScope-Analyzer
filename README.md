# MalScope-Analyzer
MalScope is an automated malware analysis tool with a GUI that scans files using static and dynamic techniques, integrates VirusTotal API, extracts indicators (IPs, URLs), and generates detailed PDF reports with threat classification.
